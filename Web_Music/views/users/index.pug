h1 List Users
a(href="/users/create") New
form(action="/users/search", method="GET")
    input(type="text", name='q')
    button Search
ul
  each user, index in users
    li
      span=index+1 + ': ' +user.name + ' - '+user.age
      span &nbsp;
      a(href="/users/" +user.id) View
  else 
    li Not found